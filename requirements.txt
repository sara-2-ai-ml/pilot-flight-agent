# Web API
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0

# Configuration
pydantic-settings>=2.6.0,<3.0
python-dotenv>=1.0.0,<2.0

# External HTTP (Lufthansa API — Phase 5)
httpx>=0.28.0,<1.0

# LLM (Planner — Phase 2)
anthropic>=0.42.0,<1.0

# MCP (Phase 6) — pin v1 to stay compatible with FastAPI/Starlette stack
mcp>=1.28.0,<2

# Testing
pytest>=8.3.0,<9.0
pytest-asyncio>=0.24.0,<1.0
